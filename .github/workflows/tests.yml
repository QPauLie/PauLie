name: tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - uses: actions/setup-python@v3
      with:
        python-version: "3.12"
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pipx install uv
        uv sync --all-extras --dev

    - name: Run tests
      run: uv run pytest

    - name: Lint
      #run: uvx pylint $(git ls-files '*.py')
      run: | 
        uvx ruff check .
        uvx pylint "src/**/*.py" "tests/*.py" "examples/*.py"
